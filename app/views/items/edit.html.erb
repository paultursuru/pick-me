<h2 class="text-xl">Make changes on this item : <%= @item.name %></h2>

<div class="rounded p-2 bg-white text-emerald-950">
  <%= turbo_frame_tag @item do %>
    <%= render "form", item: @item %>
  <% end %>
</div>
